/** Tamanhos de ícone como token — evita valores arbitrários espalhados pelo código. */
export const ICON = {
  /** Inline em botões e links de texto. */
  sm: 14,
  /** Linhas de lista e itens de contato. */
  md: 16,
} as const;
